.share-section
  = link_to 'Submit', new_post_path
  %p Share a Rails Best Practice!
.posts-section= render 'shared/navigation'
.tag-section
  %h3 Tags
  %ul
    - ActsAsTaggableOn::Tag.order("name").each do |tag|
      %li= link_to tag.name, tag_path(tag.name)
