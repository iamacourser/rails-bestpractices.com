.comments
  - comments.each do |comment|
    .comment
      = comment.body.gsub("\n", "<br/>")
      %span
        Posted by 
        = comment_user_link(comment)
        on 
        = I18n.localize(comment.created_at)
      .clear
