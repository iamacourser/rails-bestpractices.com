%h2 Post an implementation

= semantic_form_for @post.build_implementation, :url => post_implementation_path(@post) do |form|
  = render 'form', :f => form
  = form.buttons do
    = form.submit "Create"
    = link_to 'Back', post_path(@post)
