- title "implementation of #{@post.title}"

- if current_user and current_user.developer? and @implementation.belongs_to? current_user
  .command= link_to 'Edit', edit_post_implementation_path(@post)

%h2 
  Implementation of 
  = link_to @post.title, post_path(@post)

.implementation
  = render 'shared/addthis'
  %p
    = image_tag @implementation.user.gravatar_url(:size => 32), :class => 'user-avatar', :alt => @implementation.user.login
    Posted by 
    = link_to @implementation.user.login, user_path(@implementation.user)
    on 
    = l(@implementation.created_at.to_date)
  .wikistyle= raw @implementation.formatted_html

  = render 'comments/comments', :parent => @implementation
  = render 'comments/comment_form', {:parent => @implementation, :comment => @comment}

- content_for :js do
  = javascript_include_tag "http://s7.addthis.com/js/250/addthis_widget.js#username=flyerhzm"
