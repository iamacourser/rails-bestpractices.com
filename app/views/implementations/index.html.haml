%h2 Implementation of Rails Best Practices
.implementations
  - @implementations.each do |implementation|
    .implementation
      .title= link_to "implementation of #{implementation.post.title}, implementation_path(implementation)
      = link_to (implementation.comments.size == 0 ? "implementation a comment" : "#{implementation.comments.size} comments"), implementation_path(implementation, :anchor => 'comments')
      .author
        by
        = user_link(implementation.user)
      .avatar= image_tag implementation.user.gravatar_url(:size => 32), :class => 'user-avatar'

= will_paginate @implementations, :previous_label => "<< Previous", :next_label => "Next >>"
