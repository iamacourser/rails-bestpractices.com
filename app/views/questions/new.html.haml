- set_meta_tags :title => "Request a rails best practice", :noindex => true

%h2 Request a Rails Best Practice

= semantic_form_for @question do |form|
  = render 'form', :f => form
  = form.buttons do
    = form.submit "Request"
    = link_to 'Back', questions_path
