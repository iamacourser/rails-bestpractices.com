- set_meta_tags :title => 'Edit a best practice', :noindex => true

%h2 Edit a Rails Best Practice

= semantic_form_for @post do |form|
  = render 'form', :f => form
  = form.buttons do
    = form.submit "Save"
    = link_to 'Back', post_path(@post)
